{% extends 'base.html.twig' %}

{% block title %}Hello world!{% endblock %}

{% block content %}
    <h1>Hello world!</h1>

    {% if app.user %}
        <a href="{{ path('review_index') }}">Go to events list</a>

        <a href="{{ path('logout') }}">Logout</a>
    {% else %}
        <a href="{{ path('symfony_connect_login') }}">Connect with Symfony Connect</a>
    {% endif %}
{% endblock %}
